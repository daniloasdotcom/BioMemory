import 'package:flutter/material.dart';

const mainColor = Colors.pinkAccent;

const cardTextStyle = TextStyle(fontSize: 15, letterSpacing: 1.0);
const otherTextStyle = TextStyle(fontSize: 15, color: Colors.white);